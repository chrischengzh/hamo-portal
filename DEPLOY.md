# Vercel 自动部署指南

你的代码已经推送到 GitHub：
**https://github.com/chrischengzh/hamo-portal**

现在按照以下步骤设置 Vercel 自动部署，之后每次 push 到 GitHub 都会自动更新。

---

## 步骤 1：访问 Vercel Dashboard

1. 打开浏览器，访问：https://vercel.com/login
2. 使用 GitHub 账号登录（推荐）或其他方式登录

---

## 步骤 2：创建新项目

1. 登录后，点击右上角的 **"Add New..."** 按钮
2. 选择 **"Project"**
3. 或者直接访问：https://vercel.com/new

---

## 步骤 3：导入 Git 仓库

1. 在 "Import Git Repository" 页面，找到：
   ```
   chrischengzh/hamo-portal
   ```

2. 点击仓库右侧的 **"Import"** 按钮

   **如果没有看到仓库：**
   - 点击 "Adjust GitHub App Permissions"
   - 授权 Vercel 访问你的 GitHub 仓库
   - 选择 "All repositories" 或只选择 "hamo-portal"

---

## 步骤 4：配置项目（通常无需修改）

Vercel 会自动检测到这是一个 Vite 项目，并自动配置：

- **Framework Preset**: Vite
- **Root Directory**: `./` (默认)
- **Build Command**: `npm run build` (自动检测)
- **Output Directory**: `dist` (自动检测)
- **Install Command**: `npm install` (自动检测)

**你不需要修改任何设置**，保持默认即可。

---

## 步骤 5：部署

1. 检查配置无误后，点击页面底部的 **"Deploy"** 按钮
2. Vercel 会开始构建和部署你的项目
3. 等待 1-2 分钟，你会看到：
   - ✓ Building
   - ✓ Deploying
   - 🎉 Deployment Ready

---

## 步骤 6：查看部署结果

部署成功后，Vercel 会显示：

- **部署 URL**：类似 `https://hamo-portal-xxx.vercel.app`
- **生产域名**：可以在设置中配置自定义域名

点击 URL 即可访问你的应用！

---

## 🎉 自动部署已配置完成！

从现在开始，每次你：
```bash
git add .
git commit -m "更新说明"
git push
```

Vercel 会自动：
1. 检测到 GitHub 仓库的变化
2. 自动拉取最新代码
3. 自动执行 `npm install` 和 `npm run build`
4. 自动部署到生产环境
5. 更新你的网站

---

## 查看部署状态

访问 Vercel Dashboard 可以看到：
- https://vercel.com/chrischengzh/hamo-portal

在这里你可以：
- 查看所有部署历史
- 查看构建日志
- 配置环境变量
- 配置自定义域名
- 查看访问分析

---

## 配置自定义域名（可选）

1. 在 Vercel Dashboard 中，选择你的项目
2. 点击 **"Settings"** > **"Domains"**
3. 输入你的域名（如 `portal.hamo.ai`）
4. 按照提示在你的域名 DNS 设置中添加记录
5. 等待 DNS 生效（通常几分钟到几小时）

---

## 测试自动部署

1. 修改一个文件，比如 `src/App.jsx`
2. 提交并推送：
   ```bash
   git add .
   git commit -m "test: 测试自动部署"
   git push
   ```
3. 访问 Vercel Dashboard，你会看到新的部署正在进行
4. 等待完成后，刷新你的网站，会看到更新

---

## 常见问题

### Q: 构建失败怎么办？
A: 在 Vercel Dashboard 中查看构建日志，通常是依赖安装或构建命令的问题。

### Q: 如何回滚到之前的版本？
A: 在 Vercel Dashboard 的 "Deployments" 页面，找到之前的部署，点击 "..." > "Promote to Production"

### Q: 如何设置环境变量？
A: Settings > Environment Variables，添加变量后需要重新部署

---

## 下一步

访问你的 Vercel Dashboard 开始部署：
**https://vercel.com/new**

或者直接导入这个仓库：
**https://vercel.com/new/git/external?repository-url=https://github.com/chrischengzh/hamo-portal**
